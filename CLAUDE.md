# CLAUDE.md

The agent contract for this repository lives in `AGENTS.md`, so that
every coding agent reads the same document. It is imported below rather
than summarised here, to keep one copy of the rules.

@AGENTS.md
