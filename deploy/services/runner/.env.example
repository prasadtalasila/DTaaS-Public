# Copy this file to .env before starting the GitLab runner.
# Example:
#   cp deploy/services/runner/.env.example deploy/services/runner/.env
#
# Used by compose.runner.server.yml when optional self-signed certificate
# mapping is enabled.
SERVER_DNS=foo.com
