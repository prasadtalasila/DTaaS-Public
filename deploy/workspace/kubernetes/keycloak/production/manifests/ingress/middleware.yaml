apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: traefik-forward-auth
  namespace: dtaas-workspace
spec:
  forwardAuth:
    address: http://traefik-forward-auth:4181  # NOSONAR - internal cluster communication only
    authResponseHeaders:
      - X-Forwarded-User
